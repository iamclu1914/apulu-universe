# Migration notes

## JSON path references (Phase 1.2)

No hardcoded references found in `pipeline/` or `scripts/` for `research/vawn/briefings/`, `research/vawn/discovery/`, or `research/vawn/catalog/`.

Searched with ripgrep against both `{pipeline,scripts}/**/*` and the code-file subset `*.{py,js,ts,sh,ps1,json,yaml,yml,toml}` — zero matches.
