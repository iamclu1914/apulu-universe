# Migration notes

## JSON path references (Phase 1.2)

No hardcoded references found in `pipeline/` or `scripts/` for `research/vawn/briefings/`, `research/vawn/discovery/`, or `research/vawn/catalog/`.

Searched with ripgrep against both `{pipeline,scripts}/**/*` and the code-file subset `*.{py,js,ts,sh,ps1,json,yaml,yml,toml}` — zero matches.

## artists/ path references (Phase 2.1)

No hardcoded references found in `pipeline/`, `scripts/`, or `projects/` for `artists/vawn`.

Hits occur only in `docs/superpowers/plans/` and `docs/superpowers/specs/` (historical planning/design docs describing the prior state or the migration itself). These are documentation and do not require rewrites — they accurately describe the `artists/vawn/` state that existed before this migration.

## Task C discovery — research/vawn/discovery/ leftovers (2026-04-14)

After Task C (briefings/health/discovery brief moves), `research/vawn/discovery/` retained 41 files out of original scope:

- Daily platform-pipeline journals: `Instagram Pipeline --YYYY-MM-DD.md`, `TikTok Pipeline...`, `X Pipeline...`, `Reddit Pipeline...`, `YouTube Pipeline...` — these match the journal pattern and probably belong in `journals/vawn/<platform>/YYYY-MM-DD-<slug>.md`.
- Topical research pipeline docs: `Research Pipeline — <topic>.md` (no date) — likely one-off research belonging in `research/vawn/` top level.
- One standalone: `Seedance 2.0 Research — 2026-04-10.md` — belongs in research root as dated research doc.

Suggested disposition in a follow-up cleanup pass (after main plan tasks A-I complete):
- Pipeline-platform dailies → `journals/vawn/<platform>/YYYY-MM-DD-<platform>-pipeline.md`
- Research Pipeline topical → `research/vawn/<kebab-topic>.md` (with frontmatter per spec)
- Seedance standalone → `research/vawn/2026-04-10-seedance-2-research.md`

Count: 41 files.
