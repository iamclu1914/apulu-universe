# Migration notes

## JSON path references (Phase 1.2)

No hardcoded references found in `pipeline/` or `scripts/` for `research/vawn/briefings/`, `research/vawn/discovery/`, or `research/vawn/catalog/`.

Searched with ripgrep against both `{pipeline,scripts}/**/*` and the code-file subset `*.{py,js,ts,sh,ps1,json,yaml,yml,toml}` — zero matches.

## artists/ path references (Phase 2.1)

No hardcoded references found in `pipeline/`, `scripts/`, or `projects/` for `artists/vawn`.

Hits occur only in `docs/superpowers/plans/` and `docs/superpowers/specs/` (historical planning/design docs describing the prior state or the migration itself). These are documentation and do not require rewrites — they accurately describe the `artists/vawn/` state that existed before this migration.

## Task C discovery — research/vawn/discovery/ leftovers (2026-04-14)

After Task C (briefings/health/discovery brief moves), `research/vawn/discovery/` retained 41 files out of original scope:

- Daily platform-pipeline journals: `Instagram Pipeline --YYYY-MM-DD.md`, `TikTok Pipeline...`, `X Pipeline...`, `Reddit Pipeline...`, `YouTube Pipeline...` — these match the journal pattern and probably belong in `journals/vawn/<platform>/YYYY-MM-DD-<slug>.md`.
- Topical research pipeline docs: `Research Pipeline — <topic>.md` (no date) — likely one-off research belonging in `research/vawn/` top level.
- One standalone: `Seedance 2.0 Research — 2026-04-10.md` — belongs in research root as dated research doc.

Suggested disposition in a follow-up cleanup pass (after main plan tasks A-I complete):
- Pipeline-platform dailies → `journals/vawn/<platform>/YYYY-MM-DD-<platform>-pipeline.md`
- Research Pipeline topical → `research/vawn/<kebab-topic>.md` (with frontmatter per spec)
- Seedance standalone → `research/vawn/2026-04-10-seedance-2-research.md`

Count: 41 files.

## Task E deviation — frontmatter schema mixing (2026-04-14)

Existing pre-work had already added richer frontmatter to journals (21 files) and 2 of 4 research files (`title`, `tags`, `date`, `artist`, `status`). The Task E idempotent script correctly skipped these.

Resulting schema state:
- Journals (21): pre-existing `title`, `date`, `tags` (retrieval-sufficient via `tags: project/vawn` + folder path)
- Research tickets (APU-107, APU-108): new `ticket`, `type: research`, `status: closed`, `updated` added
- Research dated docs (2): pre-existing `tags`, `date`, `artist`, `status: complete`
- Hubs (7): new `type: hub`, `topic: <slug>` added

Decision: accept mixed schemas. The spec's `type:` field is redundant with folder structure for journals. Existing tags serve cross-cutting queries. Not harmonizing further unless retrieval friction surfaces.

If Claude retrieval breaks because of schema inconsistency, revisit by normalizing status values (`complete` vs `closed`) and adding `type: research` to the 2 dated research docs.

## Task I finding — known dead-link categories (2026-04-14)

Reorganization renames in Task C (moving daily briefings / discovery / health to journals/ with YYYY-MM-DD-slug.md names) broke inbound wikilinks that reference the old names.

Known dead link patterns:
- `[[Daily Briefing -- YYYY-MM-DD]]` and `[[Daily Briefing — YYYY-MM-DD]]` — should be `[[YYYY-MM-DD-daily-briefing]]`
- `[[Health -- YYYY-MM-DD]]` / `[[Health — YYYY-MM-DD]]` — should be `[[YYYY-MM-DD-health]]`
- `[[Discovery Brief -- YYYY-MM-DD]]` / `[[Discovery Brief — YYYY-MM-DD]]` — should be `[[YYYY-MM-DD-discovery-brief]]`
- `[[Instagram Pipeline -- YYYY-MM-DD]]`, `[[X Pipeline ...]]`, `[[TikTok Pipeline ...]]`, `[[YouTube Pipeline ...]]`, `[[Reddit Pipeline ...]]`, `[[Research Pipeline — <topic>]]` — these target files still live in `research/vawn/discovery/` (deferred in the earlier Task C discovery gap).
- `[[note-1]]`, `[[note-2]]`, `[[../other-topic/_index]]` — template-example placeholders (unlikely to actually appear in rewritten hubs but worth a sweep).

Recommended follow-up:
1. Run `scripts/dead-link-check.ps1` (already in repo) — was started but PowerShell recursion is slow on this tree.
2. Write a rename-aware link-updater that maps old→new names for the journal files and rewrites references.
3. Handle the 41 deferred files in `research/vawn/discovery/` (rename to journal format or move out of discovery/).
4. Spot-check hubs for template-example leftovers (`[[note-1]]` etc.).

These are NOT blockers for the reorganization being "structurally complete" — only a consistency cleanup.
